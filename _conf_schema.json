{
  "enable_runtime": {
    "description": "是否启用人格运行时主链路",
    "type": "bool",
    "default": true
  },
  "DEBUG_MODE": {
    "description": "是否输出结构化调试日志",
    "type": "bool",
    "default": true
  },
  "commit_timeout_seconds": {
    "description": "候选提交等待 after_message_sent 的超时秒数",
    "type": "int",
    "default": 15
  },
  "max_runtime_chars": {
    "description": "单轮动态注入文本的最大字符数",
    "type": "int",
    "default": 800
  },
  "max_lore_chars": {
    "description": "单轮 lore 注入的最大字符数",
    "type": "int",
    "default": 300
  },
  "lore_top_k": {
    "description": "单轮最多注入的 lore 条数",
    "type": "int",
    "default": 2
  },
  "lore_score_threshold": {
    "description": "lore 命中阈值，范围 0-1",
    "type": "float",
    "default": 0.55
  },
  "static_persona_prompt": {
    "description": "基础人格底盘 prompt，会作为 Core Persona 拼接到运行时注入中",
    "type": "text",
    "default": ""
  },
  "learning_min_batch_size": {
    "description": "自动批学习触发所需的最小 learning buffer 样本数",
    "type": "int",
    "default": 3
  },
  "deterministic_mode": {
    "description": "是否启用确定性模式；开启后跳过后台自动学习，便于固定评估和稳定回归",
    "type": "bool",
    "default": false
  }
}
